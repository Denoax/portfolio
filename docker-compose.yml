services:
  portfolio:
    container_name: manimarami-portfolio
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:18088:80"
      - "127.0.0.1:18443:443"

